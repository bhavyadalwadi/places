# BidiBuddy

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 9.0.1.

## Why this repo exists

This repo appears to be an Angular practice app centered on loading and displaying place data through a `PlaceListComponent` and a small REST service layer.

## What it does

- Bootstraps an Angular 9 app
- Loads place data through a service call
- Renders a place-list view from fetched JSON data
- Provides a starting point for a directory or discovery-style UI

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The app will automatically reload if you change any of the source files.

## Code scaffolding

Run `ng generate component component-name` to generate a new component. You can also use `ng generate directive|pipe|service|class|guard|interface|enum|module`.

## Build

Run `ng build` to build the project. The build artifacts will be stored in the `dist/` directory. Use the `--prod` flag for a production build.

## Running unit tests

Run `ng test` to execute the unit tests via [Karma](https://karma-runner.github.io).

## Running end-to-end tests

Run `ng e2e` to execute the end-to-end tests via [Protractor](http://www.protractortest.org/).

## Further help

To get more help on the Angular CLI use `ng help` or go check out the [Angular CLI README](https://github.com/angular/angular-cli/blob/master/README.md).

## Current status

- Status confidence: medium
- Maturity: Angular learning / scaffold repo
- Pending state: unknown from current repo docs

## Resume value

- Shows Angular basics, component wiring, and service-based data loading
- Best presented as a framework practice repo unless the product scope is expanded further

## Quick Repo Summary

- Purpose: This project was generated with Angular CLI version 9.0.1.
- Stack: Node.js, TypeScript, CSS, JavaScript
- Status confidence: medium
- Pending: unknown from current repo docs

## LLM Start Here
- `README.md`
- `graphify-out/GRAPH_REPORT.md`
- `package.json`
- `src/app`
- `graphify-out/repo-semantic-summary.md`
